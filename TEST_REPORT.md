# GAC Engine Test Report

_Auto-generated by `node test/gac-sim/run.mjs`. Re-run the harness to refresh this file -- don't hand-edit it. Last run: 2026-09-26T06:53:50.909Z._

## Scope

This harness drives `gac-engine.js` directly (pure state/logic, no DOM). It covers night resolution, role reassignment (Wet steal, Krampus convert), and win-checking.

**Known gap -- not covered by this pass:** the day-vote pipeline, Charlie Brown's exact-tie death, Bad Santa/Scrooge self-elimination for breaking silence, and Jack Frost's revenge kill all live in `index.html` (DOM-coupled), not in `gac-engine.js`. "Days" in this harness are a synthetic stand-in (random elimination via the exported `eliminate()`, or no elimination) purely to advance state between nights so the afterVote-gated parity win gets exercised. This is a placeholder for driving state transitions, not a test of real day-vote/tie/self-elimination rules. A follow-up pass would need to either extract those functions from `index.html` into something DOM-free, or drive a headless DOM.

## How to reproduce any result below

Every Monte Carlo game is seeded. Copy the command shown next to a finding and run it -- it replays that exact game night-by-night and dumps the final state.

## Monte Carlo summary

- Coverage matrix: 192 games (every combination of player count 5/6/7/8/10/12/16/20, win mode majority/total, 1-3 basic Grinch cards, Bad Santa in/out, Krampus in/out -- waking/special role subsets randomized per seed).
- Random batch: 2000 fully-random games.
- Total: 2192 games.

Outcome distribution:

- christmas: 860 (39.2%)
- grinch: 1264 (57.7%)
- lovers: 16 (0.7%)
- draw: 52 (2.4%)

No stalemates -- every simulated game reached a winner within 40 nights.

## Rule-invariant violations found during Monte Carlo runs

None. Every invariant checked (Sam excluded from living(), lovers share fate, once-per-game powers never double-fire, win results match the team counts that justify them, parity wins respect `gacChristmasHasPathToVictory` plus the lover-pair/Frost short-circuits, total-mode never ends early) held across all games above.

## Targeted scenarios (hand-built, exact expectations)

33/33 passed.

### S1. PASS -- Elf protection blocks a Grinch kill AND makes a simultaneous Krampus convert attempt fail (power still spent)

- **Manual/spec reference:** Elf on the Shelf: "That player cannot be killed that night." CLAUDE.md: Krampus "fails if the victim is protected/saved/shielded."
- **Result:** victim.alive=true, krampus convert power remaining=false, "convert failed" logged=true

### S2. PASS -- Offering the cookie to an already-protected victim is "wasted" -- the power is still spent

- **Manual/spec reference:** Mrs. Claus how-it-plays: her save is one-time, spent whether or not it was needed.
- **Result:** victim.alive=true, save power remaining=false, "save wasted" logged=true

### S3. PASS -- A shield already spent by the Yukon card does not refresh when Wet Bandits steal that card

- **Manual/spec reference:** CLAUDE.md: shields belong to the CARD, and spent state travels with it (game.spentShields).
- **Result:** precondition spentShields.Yukon=true; after steal: newHolder.roleId=Yukon, newHolder.shieldCount=0

### S4. PASS -- A player targeted by both the Grinches and Belsnickel the same night dies exactly once, attributed to whichever source resolves last

- **Manual/spec reference:** Not stated in the manual (nobody expects to be targeted twice) -- documenting actual behavior.
- **Result:** deaths=[{"playerId":"p2","name":"Victim","source":"belsnickel"}]. Not a bug: one death either way. Flagging only because if a "cause of death" is ever shown, it will say Belsnickel (step 5) and never mention the Grinches also chose them (step 2).

### S5. PASS -- Cross-team lovers left as the final two win as lovers, even when that board also satisfies the Grinch parity condition

- **Manual/spec reference:** Manual: "if Cupid linked two players from opposite teams and those two are the last two alive, their love wins out over either team."
- **Result:** checkWin result=lovers (grinch=1, lover-team=christmas, so parity would also read as a Grinch win if lovers weren't checked first)

### S6. PASS -- Grinch parity win is withheld while Christmas can still kill overnight (Belsnickel alive, unused), and granted once that's no longer true

- **Manual/spec reference:** gac-engine.js gacChristmasHasPathToVictory() -- see finding F8 for the manual-ambiguity writeup.
- **Result:** 1 Grinch vs 1 Belsnickel (unused): result=null; 1 Grinch vs 1 plain Elf: result=grinch

### S7. PASS -- living() excludes a player misdealt the Sam card, unconditionally

- **Manual/spec reference:** CLAUDE.md: "living() EXCLUDES roleId === 'Sam'. Sam is the moderator, never a player."
- **Result:** living() length=1, contents=Elf1

### S8. PASS -- The last Grinch and the last Christmas defender killing each other the same night ends in a draw, not a hang

- **Manual/spec reference:** gac-engine.js comment: "e.g. a Grinch and Belsnickel kill each other on the final night -> nobody wins."
- **Result:** checkWin result=draw, living count=0

### S9. PASS -- Mrs. Claus's save cannot fire a second time once used, even across multiple nights

- **Manual/spec reference:** Manual: her save is a one-time power, once each all game.
- **Result:** night 1 save worked=true (v1.alive=true), night 2 save ignored=true (v2.alive=false)

### S14. PASS -- Krampus cannot convert a victim whose Yukon shield is about to absorb the attack -- the convert fails and the power is still spent

- **Manual/spec reference:** CLAUDE.md: Krampus "fails if the victim is protected/saved/shielded."
- **Result:** yukon.alive=true, yukon.team=christmas, yukon.shieldCount=0, krampus power remaining=false

### S15. PASS -- Buddy's swap moves BOTH the incoming attack and the Elf's protection to the new seat, not the person

- **Manual/spec reference:** Manual: "Any fate aimed at one of them that night -- a Grinch kill, a poison, anything -- follows the swap and lands on the other instead."
- **Result:** A (was protected, swapped into B's targeted-but-unprotected seat): alive=false (expected false). B (was targeted, swapped into A's protected seat): alive=true (expected true).

### S16. PASS -- The Grinches can kill one of their own -- no built-in teammate immunity

- **Manual/spec reference:** CLAUDE.md: "They may target any living player, including another Grinch."
- **Result:** target(Grinch2).alive=false

### S17. PASS -- In "total" win mode, reaching numeric parity does not end the game early

- **Manual/spec reference:** Manual: "(In a total-elimination game, they instead win only once the entire Christmas team is gone.)"
- **Result:** 1 Grinch vs 1 Elf under gacWinMode:"total": checkWin result=null (expected null)

### S18. PASS -- A stale belsnickelKill decision is a no-op once Belsnickel is dead

- **Manual/spec reference:** Engine hardening (F1 resolved): the engine no longer trusts a kill decision with no living actor behind it.
- **Result:** target.alive=true (expected true -- kill did not land)

### S19. PASS -- A stale protect decision is a no-op once Elf on the Shelf is dead

- **Manual/spec reference:** Engine hardening (F2 resolved).
- **Result:** target.alive=false (expected false -- protection did not apply, Grinch kill landed)

### S20. PASS -- A stale grinchKill decision is a no-op with zero living Grinches

- **Manual/spec reference:** Engine hardening (F4 resolved).
- **Result:** target.alive=true (expected true)

### S21. PASS -- cupidLink is ignored on any night after night 1

- **Manual/spec reference:** Engine hardening (F5 resolved, Cupid half). Manual: Cupid links on night 1 only.
- **Result:** a.loverOf=null, b.loverOf=null (expected both null)

### S22. PASS -- wetSteal is ignored on any night after night 1

- **Manual/spec reference:** Engine hardening (F5 resolved, Wet half). Manual: Wet Bandits steal on night 1 only.
- **Result:** wet.roleId=Wet, target.roleId=Elf1 (expected unchanged)

### S23. PASS -- Elf on the Shelf protecting themselves is rejected -- the protection is wasted, the Grinch kill lands

- **Manual/spec reference:** Manual: "never themselves." Engine hardening (F6 resolved, self half).
- **Result:** shelf.alive=false (expected false), "protect wasted" logged=true

### S24. PASS -- Elf on the Shelf protecting the same player two nights running is rejected on the second night -- wasted, not applied

- **Manual/spec reference:** Manual: "cannot protect the same player two nights in a row." Engine hardening (F6 resolved, repeat half).
- **Result:** target.alive=false (expected false -- night 2 protection wasted, Grinch kill landed), "protect wasted" logged=true

### S25. PASS -- Once Santa is dead, the naughty/nice check is attributed to Scott Calvin, not a null-actor "Santa Claus" entry

- **Manual/spec reference:** Manual: Calvin inherits Santa's nightly check. Engine hardening (F3 resolved).
- **Result:** logged event={"night":1,"power":"Scott Calvin","actorId":"p1","targetId":"p2","result":"checked:nice","actorRole":"Calvin","targetRole":"Elf1"}

### S26. PASS -- Mrs. Claus cannot save herself -- the attempt is wasted (mirrors the Elf's own self-protect rule)

- **Manual/spec reference:** Engine bug fix, confirmed by Scott: the save block previously had no self-check at all.
- **Result:** mrs.alive=false (expected false), save power remaining=false, "save wasted" logged=true

### G1. PASS -- 3 Grinch vs 2 Christmas, Yukon's shield intact but purely defensive (no kill tool) -> GRINCH WIN

- **Manual/spec reference:** Corrected per Scott: a defensive-only resource can never lower the Grinch count, so no path exists.
- **Result:** checkWin=grinch (expected "grinch")

### G2. PASS -- 3 Grinch vs 2 Christmas, Mrs. Claus's poison unused -> CONTINUE

- **Manual/spec reference:** New Grinch-win design (not yet implemented): an unused poison is a still-open Christmas path.
- **Result:** checkWin=null (expected null)

### G3. PASS -- 3 Grinch vs 2 Christmas, no powers left, no lovers -> GRINCH WIN

- **Manual/spec reference:** New Grinch-win design: no simulable Christmas mechanism remains -> no path -> Grinch win.
- **Result:** checkWin=grinch (expected "grinch")

### G4. PASS -- Cross-team lovers alive anywhere on the board (4 living, not the final two) -> CONTINUE via short-circuit

- **Manual/spec reference:** Scott's correction: "any cross-team lover pair still alive -> CONTINUE" -- a simple short-circuit, ordered right after the existing final-two lovers-win check and alongside the Frost carve-out, before the reachability search.
- **Result:** checkWin=null (expected null) -- board: 2 Grinch, 2 Christmas (1 Grinch + 1 Christmas member cross-linked as lovers, NOT the only two alive)

### G5. PASS -- A second no-path board shape (2 Grinch vs 1 Elf, no powers) -> GRINCH WIN

- **Manual/spec reference:** New Grinch-win design: a differently-sized/shaped no-path board should resolve the same way as G3.
- **Result:** checkWin=grinch (expected "grinch")

### G6. PASS -- Christmas never wins early -- 1 Grinch alive is still a live game, even heavily outnumbered

- **Manual/spec reference:** Scott: "Christmas has NO early win -- Christmas only wins when zero Grinches remain."
- **Result:** checkWin=null (expected null)

### G7. PASS -- 3 Grinch vs 2 Christmas, no simulable powers, Frost alive -> CONTINUE (hardcoded carve-out, per Scott's decision)

- **Manual/spec reference:** Frost's revenge lives in index.html, not simulable via resolveNight() -- kept as a hardcoded 'path exists' carve-out.
- **Result:** checkWin=null (expected null)

### G8. PASS -- Exact tie (2v2) with Elf's protect available -> CONTINUE (survives the one night, the tie-vote then removes a Grinch)

- **Manual/spec reference:** Scott's confirmed model: at an exact tie, any blocking resource preserves it into a vote that succeeds.
- **Result:** checkWin=null (expected null)

### G9. PASS -- Exact tie (2v2), no powers at all -> GRINCH WIN (the night kill lands unblocked, breaking the tie before any vote)

- **Manual/spec reference:** Scott's confirmed model: without any blocking, the tie doesn't survive to the vote.
- **Result:** checkWin=grinch (expected "grinch")

### G10. PASS -- Exact tie (1v1) where Shelf is the ONLY living Other -> GRINCH WIN (can't protect himself, needs another target)

- **Manual/spec reference:** Scott's confirmed model: Elf's protect (like Mrs's save) requires a living Other besides the holder.
- **Result:** checkWin=grinch (expected "grinch")

### G11. PASS -- Exact tie (2v2) with Mrs. Claus's save available (and another living Other to save) -> CONTINUE

- **Manual/spec reference:** Scott's confirmed model, and confirms the self-save fix doesn't block the case where another target exists.
- **Result:** checkWin=null (expected null)

## Discrepancies found (for your judgment -- not changed)

These are real, reproducible behaviors of `gac-engine.js` that either don't match the manual's plain-English wording, the manual doesn't address at all, or don't match the engine's own documentation. None of these have been changed -- sorted below into three buckets so you can go through them by kind.

### Likely code bugs

_A real gap in gac-engine.js itself, independent of any rules question -- fixable without needing a ruling from you first._

(none)

### Likely stale doc/comment

_The running code is fine (or fine enough); a comment or another piece of code describing it is wrong or out of date._

(none)

### Ambiguous -- needs your rules/design call

_Behavior that depends on an intent question only you can settle: is gac-engine.js meant to enforce every rule itself, or is index.html's UI the sole gatekeeper by design (with the engine trusting whatever it's handed)? Also includes places where the engine does something reasonable-sounding that the printed manual simply doesn't document._

#### F8. The Grinch-win reachability rule is real, deliberate engine behavior, but none of it is written down anywhere a player would read it

- **Where:** gac-engine.js gacChristmasHasPathToVictory() and checkWin() step 3.
- **What happens:** The printed manual says the Grinches win "the moment they equal or outnumber" the table. The engine actually runs a considerably more precise rule (designed and confirmed with Scott, replacing an earlier simpler heuristic): a living cross-team lover pair or a living Jack Frost always continues the game; otherwise, an unused offensive tool (Mrs. Claus's poison, a living Belsnickel, or Buddy's unused swap) always continues it; otherwise, at an EXACT tie a single blocking resource (Elf's protect or Mrs's save, each needing a living target besides themselves, or Yukon's shield) continues it by holding the tie into the next vote; a strict Grinch lead can never be rescued by defense alone, however much of it exists. None of this nuance is in GAC_Manual.pdf. Worth deciding: should the manual describe it (at whatever level of detail makes sense for players), or is this meant to stay an implementation detail?

## Fuzz / robustness pass

300 games fed deliberately malformed decisions each night (dead actors, dead/nonexistent targets, empty strings, degenerate pairs) for 8 nights each.

No crashes -- resolveNight()/checkWin() never threw on any malformed input tried.

No structural corruption (no duplicate death log entries, no negative shields, no non-boolean `alive`) found either.
